import {
  formatDate
} from 'streaming-stocks/helpers/format-date';

module('FormatDateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatDate(42);
  ok(result);
});
