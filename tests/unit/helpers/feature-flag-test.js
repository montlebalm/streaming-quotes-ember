import {
  featureFlag
} from 'streaming-stocks/helpers/feature-flag';

module('FeatureFlagHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = featureFlag(42);
  ok(result);
});
