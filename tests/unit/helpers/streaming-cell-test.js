import {
  streamingCell
} from 'streaming-stocks/helpers/streaming-cell';

module('StreamingCellHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = streamingCell(42);
  ok(result);
});
